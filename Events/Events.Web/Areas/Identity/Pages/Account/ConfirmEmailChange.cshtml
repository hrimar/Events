@page
@model ConfirmEmailChangeModel
@{
    ViewData["Title"] = "Confirm Email Change";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-5 col-md-7">
            <div class="card shadow-sm border-0">
                <div class="card-body p-5">
                    <div class="text-center">
                        <i class="fas fa-envelope-open-text fa-3x text-success mb-3"></i>
                        <h2 class="fw-bold">@ViewData["Title"]</h2>
                        <p class="text-muted mb-4">Confirming your new email address</p>

                        <div class="mt-4">
                            <partial name="_StatusMessage" model="Model.StatusMessage" />
                        </div>

                        <hr class="my-4">

                        <div class="text-center">
                            <a asp-page="/Account/Manage/Index" class="text-decoration-none">
                                <i class="fas fa-arrow-left me-1"></i>Back to Account Settings
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
